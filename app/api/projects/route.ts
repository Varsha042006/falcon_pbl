import { NextResponse } from 'next/server';import { requireAnyRole } from '@/lib/auth';import { query } from '@/lib/db';
export async function POST(req:Request){const u=await requireAnyRole(['FACULTY']);if(!u||!u.facultyId)return NextResponse.redirect(new URL('/login',req.url),303);const f=await req.formData();const pblCycleId=Number(f.get('pblCycleId'));const n=(await query<{n:number}>('SELECT COUNT(*)::int+1 n FROM projects_v2 WHERE pbl_cycle_id=$1',[pblCycleId]))[0]?.n||1;const code=`PBL-P-${pblCycleId}-${String(n).padStart(4,'0')}`;await query(`INSERT INTO projects_v2(pbl_cycle_id,project_code,title,short_description,description,problem_statement,expected_outcome,domain,technology_stack,project_type,owner_faculty_id,guide_faculty_id,max_teams,application_mode,status,published_at) VALUES($1,$2,$3,$4,$5,$6,$7,$8,$9,'FACULTY_PROJECT',$10,$10,$11,'OPEN','PUBLISHED',NOW())`,[pblCycleId,code,String(f.get('title')),String(f.get('shortDescription')),String(f.get('description')),String(f.get('problemStatement')||''),String(f.get('expectedOutcome')||''),String(f.get('domain')||''),String(f.get('technologyStack')||''),u.facultyId,Number(f.get('maxTeams')||1)]);return NextResponse.redirect(new URL('/projects',req.url),303)}
