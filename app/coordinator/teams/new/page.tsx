import { requireAnyRole } from '@/lib/auth';import { query } from '@/lib/db';import { redirect } from 'next/navigation';
export default async function NewTeam({searchParams}:{searchParams:Promise<{cycle?:string}>}){const u=await requireAnyRole(['PBL_COORDINATOR','HOD']);if(!u)redirect('/login');await query('UPDATE pbl_cycles_v2 SET team_min_size=5, team_max_size=6 WHERE team_min_size=3 AND team_max_size=4').catch(()=>{});const q=await searchParams;const cycles=await query<any>(`SELECT pc.id,pc.semester,pc.team_min_size,pc.team_max_size,ac.name FROM pbl_cycles_v2 pc JOIN academic_cycles_v2 ac ON ac.id=pc.academic_cycle_id LEFT JOIN pbl_coordinator_assignments_v2 ca ON ca.id=pc.coordinator_assignment_id WHERE $1::boolean OR ca.faculty_id=$2 ORDER BY pc.semester`,[u.roles.includes('HOD'),u.facultyId||null]);const cycleId=Number(q.cycle||cycles[0]?.id||0);const cycle=cycles.find(c=>c.id===cycleId);const students=cycle?await query<any>(`SELECT e.id enrollment_id,s.usn,s.name,se.code section_code,f.name mentor_name FROM student_enrollments_v2 e JOIN students_v2 s ON s.id=e.student_id JOIN sections_v2 se ON se.id=e.section_id LEFT JOIN faculty_v2 f ON f.id=s.mentor_faculty_id LEFT JOIN team_members_v2 tm ON tm.enrollment_id=e.id AND tm.pbl_cycle_id=$1 WHERE e.academic_cycle_id=(SELECT academic_cycle_id FROM pbl_cycles_v2 WHERE id=$1) AND e.program_id=(SELECT program_id FROM pbl_cycles_v2 WHERE id=$1) AND e.semester=(SELECT semester FROM pbl_cycles_v2 WHERE id=$1) AND tm.id IS NULL ORDER BY se.code,s.usn`,[cycleId]):[];return <section className="section"><div className="container"><h1>Create PBL Team</h1><form method="get"><label>PBL Cycle / Semester </label><select name="cycle" defaultValue={cycleId}>{cycles.map(c=><option key={c.id} value={c.id}>{c.name} — Semester {c.semester}</option>)}</select> <button className="btn secondary">Load</button></form>{cycle&&<div className="notice">Team rule: minimum {cycle.team_min_size}, maximum {cycle.team_max_size}. Students are restricted to this academic cycle and Semester {cycle.semester}; students already in a team are hidden.</div>} {cycle&&<div className="form"><form action="/api/teams" method="post"><input type="hidden" name="pblCycleId" value={cycleId}/><div className="field"><label>Team name</label><input name="teamName" required/></div><div className="field"><label>Select students</label>{students.map(s=><label key={s.enrollment_id} style={{display:'block',padding:'7px'}}><input type="checkbox" name="enrollmentIds" value={s.enrollment_id}/> {s.usn} — {s.name} ({s.section_code}) · Mentor: {s.mentor_name||'—'}</label>)}</div><div className="field"><label>Team leader</label><select name="leaderEnrollmentId" required><option value="">Select one of the chosen students</option>{students.map(s=><option key={s.enrollment_id} value={s.enrollment_id}>{s.usn} — {s.name}</option>)}</select></div><button className="btn">Create & Activate Team</button></form></div>}</div></section>}
